---
import HeroPortrait from "./HeroPortrait.astro";
import AvailabilityBadge from "@components/ui/AvailabilityBadge.astro";
import CtaButton from "@components/ui/CtaButton.astro";
---

<section class="hero" aria-label="Introduction">
  <div class="content">
    <div class="text-side">
      <div class="title-block">
        <h1 class="name">
          <span class="name-main">JULIEN</span>
          <span class="name-outline">MARCHAND</span>
        </h1>

        <p class="subtitle">
          Développeur web <em class="highlight">full-stack</em>.
        </p>
      </div>

      <!-- <AvailabilityBadge status="available" /> -->
    </div>
    <div class="portrait-area">
      <HeroPortrait />
    </div>
    <div class="actions">
      <CtaButton href="/#projets" label="Projets" corners />
      <CtaButton href="/#contact" label="Me contacter" corners />
    </div>
  </div>
</section>

<style lang="scss">
  @use "@styles/config/variables" as *;
  @use "@styles/config/mixin" as *;

  .hero {
    position: relative;
    display: flex;
    align-items: center;
    min-height: 100vh;
    overflow: hidden;
    padding: 6rem 1.5rem 2rem 1.5rem;

    @include breakpoint-up($breakpoint-desktop) {
      padding: 10rem 3rem 1rem 3rem;
    }

    @include breakpoint-up($breakpoint-desktop-xl) {
      padding: 8rem 2rem 1rem 2rem;
    }
  }

  .content {
    display: grid;
    grid-template-rows: 30% 55% 15%;
    align-items: center;
    position: relative;
    min-height: 70vh;
    z-index: 10;
    max-width: 80rem;
    margin: 0 auto;
    width: 100%;

    @include breakpoint-up($breakpoint-desktop) {
      grid-template-columns: 1fr 2fr;
      // Rows au contenu : les actions restent collées sous le bloc texte,
      // l'ensemble est centré verticalement dans le hero.
      grid-template-rows: auto auto;
      align-content: center;
    }
  }

  .title-block {
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    justify-content: space-between;
    gap: 0.5rem;
  }

  .name {
    font-size: clamp(3rem, 15vw, 8rem);
    line-height: 0.85;
    letter-spacing: -0.03em;
    font-style: italic;
    margin: 0 auto;
    @include breakpoint-up($breakpoint-desktop) {
      font-size: clamp(1rem, 9vw, 8rem);
    }
  }

  .name-main {
    background: linear-gradient(
      to right,
      var(--text-primary),
      var(--accent-primary)
    );
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    padding-right: 0.3em;
  }

  .name-outline {
    -webkit-text-stroke: 1.5px var(--accent-primary);
    color: transparent;
    display: inline-block;
    margin-top: -0.5rem;

    @include breakpoint-up($breakpoint-desktop) {
      margin-top: -1rem;
    }
  }

  .subtitle {
    font-size: 1rem;
    font-weight: 200;
  }

  .highlight {
    color: var(--accent-primary);
  }

  .actions {
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: space-evenly;
    gap: 2rem;
    margin-top: 2rem;

    @include breakpoint-up($breakpoint-tablet) {
      gap: 3rem;
    }

    @include breakpoint-up($breakpoint-desktop) {
      grid-column: 1;
      align-self: start;
    }
  }

  .portrait-area {
    display: flex;
    align-items: center;
    justify-content: center;

    @include breakpoint-up($breakpoint-desktop) {
      grid-column: 2;
      grid-row: 1 / -1;
    }
  }
</style>
