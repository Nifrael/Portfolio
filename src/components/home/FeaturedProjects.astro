---
import FeaturedProject from "@components/home/FeaturedProject.astro";
import SectionTitle from "@components/ui/SectionTitle.astro";
import CtaButton from "@components/ui/CtaButton.astro";
import type { Project } from "@utils/projects";
import { PROJECT_SHAPES } from "../../content.config";

/**
 * FeaturedProjects — Section des projets mis en avant sur la page d'accueil.
 * Chaque projet est associé à un symbole (frontmatter `shape`, sinon cycle
 * △ ○ × □ selon sa position), en alternance visuel/texte.
 *
 * @prop {Project[]} projects - Projets à afficher (fournis par la page).
 */
interface Props {
  projects: Project[];
}

const { projects } = Astro.props;
---

<section class="featured-projects" id="projets" aria-label="Projets phares">
  <div class="section-header">
    <SectionTitle outline="QUELQUES" main="PROJETS" class="reveal-title" />
    <p class="section-subtitle">
      Sélection de réalisations techniques axées sur la performance et
      l'architecture logicielle.
    </p>
  </div>

  <div class="projects-showcase">
    {
      projects.map((project, index) => (
        <FeaturedProject
          project={project}
          shape={
            project.data.shape ?? PROJECT_SHAPES[index % PROJECT_SHAPES.length]
          }
          reverse={index % 2 === 1}
        />
      ))
    }
  </div>

  <div class="all-projects">
    <CtaButton
      href="/projects"
      label="Voir tous les projets"
      variant="outline"
      arrow
    />
  </div>
</section>

<style lang="scss">
  @use "@styles/config/variables" as *;
  @use "@styles/config/mixin" as *;

  .featured-projects {
    position: relative;
    padding: 3rem 1.5rem;
    background: transparent;
    max-width: 95%;
    margin: 0 auto;

    @include breakpoint-up($breakpoint-desktop) {
      padding: 6rem 5rem;
    }
  }

  .section-header {
    margin-bottom: 5rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;

    @include breakpoint-up($breakpoint-tablet) {
      flex-direction: row;
      margin-bottom: 8rem;
      align-items: flex-end;
      justify-content: space-between;
      gap: 3rem;
    }
  }

  .section-subtitle {
    color: var(--text-primary);
    font-size: 1rem;
    font-family: var(--font-body);
    font-weight: 200;
    line-height: 1.5;
    margin: 0;
    opacity: 0.8;
  }

  .projects-showcase {
    display: grid;
    grid-template-columns: 1fr;
    gap: 4rem;

    @include breakpoint-up($breakpoint-desktop) {
      gap: 5rem;
    }
  }

  .all-projects {
    display: flex;
    justify-content: center;
    margin-top: 5rem;
  }

  @keyframes reveal-blur {
    from {
      opacity: 0;
      filter: blur(15px);
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      filter: blur(0);
      transform: translateY(0);
    }
  }

  @supports (animation-timeline: view()) {
    // :global() requis : la classe est portée par le composant enfant
    // SectionTitle, qui ne reçoit pas l'attribut de scope de ce composant.
    .section-header :global(.reveal-title) {
      animation: reveal-blur both;
      animation-timeline: view();
      animation-range: entry 10% cover 40%;
    }

    .section-subtitle {
      animation: reveal-blur both;
      animation-timeline: view();
      animation-range: entry 15% cover 45%;
    }
  }
</style>
